name: Build WordPress Plugin ZIP

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build and attach plugin ZIP
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Prepare plugin folder
        run: |
          PLUGIN_SLUG="bloglogistics-limited-blog-writing-access"
          BUILD_DIR="/tmp/${PLUGIN_SLUG}"

          rm -rf "${BUILD_DIR}"
          mkdir -p "${BUILD_DIR}"

          rsync -av \
            --exclude=".git" \
            --exclude=".github" \
            --exclude="${PLUGIN_SLUG}.zip" \
            ./ "${BUILD_DIR}/"

      - name: Create ZIP
        run: |
          PLUGIN_SLUG="bloglogistics-limited-blog-writing-access"
          cd /tmp
          zip -r "${GITHUB_WORKSPACE}/${PLUGIN_SLUG}.zip" "${PLUGIN_SLUG}"

      - name: Upload ZIP to release
        if: github.event_name == 'release'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PLUGIN_SLUG="bloglogistics-limited-blog-writing-access"
          gh release upload "${{ github.event.release.tag_name }}" "${PLUGIN_SLUG}.zip" --clobber

      - name: Upload ZIP as workflow artifact
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: bloglogistics-limited-blog-writing-access
          path: bloglogistics-limited-blog-writing-access.zip

      - name: Build update manifest
        if: github.event_name == 'release'
        run: |
          python3 <<'PY'
          import datetime
          import html
          import json
          import re
          from pathlib import Path

          plugin_name = "BlogLogistics Limited Blog Writing Access"
          plugin_slug = "bloglogistics-limited-blog-writing-access"
          repo_url = "https://github.com/bloglogisticsdev/bloglogistics-limited-blog-writing-access"
          tag_name = "${{ github.event.release.tag_name }}"
          version = tag_name.lstrip("v")
          download_url = f"{repo_url}/releases/download/v{version}/{plugin_slug}.zip"
          last_updated = datetime.datetime.utcnow().strftime("%Y-%m-%d")

          readme_path = Path("readme.txt")
          readme = readme_path.read_text(encoding="utf-8", errors="replace")

          def extract_readme_section(readme_text, section_name):
              match = re.search(
                  rf"^==\s*{re.escape(section_name)}\s*==\s*(.*?)(?=^==\s+|\Z)",
                  readme_text,
                  flags=re.IGNORECASE | re.MULTILINE | re.DOTALL,
              )
              return match.group(1).strip() if match else ""

          def convert_simple_readme_section_to_html(section_text):
              html_parts = []
              list_items = []
              list_type = None
              paragraph_lines = []

              def flush_paragraph():
                  if paragraph_lines:
                      paragraph = " ".join(line.strip() for line in paragraph_lines if line.strip())
                      if paragraph:
                          html_parts.append(f"<p>{html.escape(paragraph)}</p>")
                      paragraph_lines.clear()

              def flush_list():
                  nonlocal list_type
                  if list_items:
                      tag = "ol" if list_type == "ol" else "ul"
                      html_parts.append(f"<{tag}>")
                      for item in list_items:
                          html_parts.append(f"<li>{html.escape(item)}</li>")
                      html_parts.append(f"</{tag}>")
                      list_items.clear()
                  list_type = None

              for raw_line in section_text.splitlines():
                  line = raw_line.strip()

                  if not line:
                      flush_paragraph()
                      flush_list()
                      continue

                  numbered_item = re.match(r"^\d+\.\s+(.*)$", line)
                  bullet_item = re.match(r"^\*\s+(.*)$", line)

                  if numbered_item:
                      flush_paragraph()
                      if list_type not in (None, "ol"):
                          flush_list()
                      list_type = "ol"
                      list_items.append(numbered_item.group(1).strip())
                      continue

                  if bullet_item:
                      flush_paragraph()
                      if list_type not in (None, "ul"):
                          flush_list()
                      list_type = "ul"
                      list_items.append(bullet_item.group(1).strip())
                      continue

                  flush_list()
                  paragraph_lines.append(line)

              flush_paragraph()
              flush_list()

              return "".join(html_parts)

          def convert_faq_to_html(faq_text):
              html_parts = []
              current_question = None
              current_answer = []

              def flush_question():
                  nonlocal current_question, current_answer

                  if not current_question:
                      return

                  answer_html = convert_simple_readme_section_to_html("\n".join(current_answer))
                  html_parts.append(f"<h4>{html.escape(current_question)}</h4>")
                  html_parts.append(answer_html if answer_html else "<p></p>")

                  current_question = None
                  current_answer = []

              for raw_line in faq_text.splitlines():
                  line = raw_line.strip()

                  question_match = re.match(r"^=\s*(.*?)\s*=$", line)
                  if question_match:
                      flush_question()
                      current_question = question_match.group(1).strip()
                      current_answer = []
                      continue

                  if current_question:
                      current_answer.append(raw_line)

              flush_question()

              return "".join(html_parts)

          def convert_changelog_to_html(changelog_text):
              html_parts = []
              current_items = []

              def flush_items():
                  nonlocal_items = list(current_items)
                  current_items.clear()
                  if nonlocal_items:
                      html_parts.append("<ul>")
                      for item in nonlocal_items:
                          html_parts.append(f"<li>{html.escape(item)}</li>")
                      html_parts.append("</ul>")

              for raw_line in changelog_text.splitlines():
                  line = raw_line.strip()
                  if not line:
                      continue

                  version_heading = re.match(r"^=\s*(.*?)\s*=$", line)
                  if version_heading:
                      flush_items()
                      html_parts.append(f"<h4>{html.escape(version_heading.group(1))}</h4>")
                      continue

                  if line.startswith("*"):
                      current_items.append(line.lstrip("*").strip())
                      continue

                  current_items.append(line)

              flush_items()

              return "".join(html_parts)

          installation_text = extract_readme_section(readme, "Installation")
          faq_text = extract_readme_section(readme, "Frequently Asked Questions")
          changelog_text = extract_readme_section(readme, "Changelog")

          installation_html = convert_simple_readme_section_to_html(installation_text) or "<p>No installation instructions available.</p>"
          faq_html = convert_faq_to_html(faq_text) or "<p>No FAQ available.</p>"
          changelog_html = convert_changelog_to_html(changelog_text) or "<p>No changelog available.</p>"

          manifest = {
              "name": plugin_name,
              "version": version,
              "download_url": download_url,
              "homepage": repo_url,
              "author": "<a href=\"https://www.bloglogistics.com\" target=\"_blank\" rel=\"noopener noreferrer\">BlogLogistics</a>",
              "author_profile": "https://www.bloglogistics.com",
              "requires": "7.0",
              "tested": "7.0",
              "requires_php": "8.3",
              "last_updated": last_updated,
              "icons": {
                  "1x": "https://updates.bloglogistics.com/plugins/assets/icon-128x128.png",
                  "2x": "https://updates.bloglogistics.com/plugins/assets/icon-256x256.png",
                  "default": "https://updates.bloglogistics.com/plugins/assets/icon-128x128.png",
              },
              "banners": {
                  "low": "https://updates.bloglogistics.com/plugins/assets/banner-772x250.png",
                  "high": "https://updates.bloglogistics.com/plugins/assets/banner-1544x500.png",
                  "default": "https://updates.bloglogistics.com/plugins/assets/banner-772x250.png",
              },
              "sections": {
                  "description": "Allows selected writing roles to create blog posts while preventing media access, uploads, publishing, comment moderation, and broader wp-admin access.",
                  "installation": installation_html,
                  "faq": faq_html,
                  "changelog": changelog_html,
              },
          }

          output_path = Path(f"{plugin_slug}.json")
          output_path.write_text(
              json.dumps(manifest, ensure_ascii=False, indent=2) + "\n",
              encoding="utf-8",
          )

          print(output_path.read_text(encoding="utf-8"))
          PY

      - name: Upload update manifest by FTPS
        if: github.event_name == 'release'
        env:
          FTP_SERVER: ${{ secrets.BLOGLOGISTICS_UPDATES_FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.BLOGLOGISTICS_UPDATES_FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.BLOGLOGISTICS_UPDATES_FTP_PASSWORD }}
          FTP_SERVER_DIR: ${{ secrets.BLOGLOGISTICS_UPDATES_FTP_SERVER_DIR }}
        run: |
          PLUGIN_SLUG="bloglogistics-limited-blog-writing-access"

          sudo apt-get update
          sudo apt-get install -y lftp

          lftp -u "${FTP_USERNAME}","${FTP_PASSWORD}" "${FTP_SERVER}" <<EOF
          set ftp:ssl-force true
          set ftp:ssl-protect-data true
          set ssl:verify-certificate false
          cd "${FTP_SERVER_DIR}"
          put "${PLUGIN_SLUG}.json" -o "${PLUGIN_SLUG}.json"
          bye
          EOF
